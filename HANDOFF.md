# 引き継ぎ指示書（高橋社長のPCで最初に読むもの）

このファイルは **Codex への指示書**です。高橋社長のPCで Codex を開き、
「HANDOFF.md を実行して」と言われたらこの手順どおりに進めてください。
**社長はコードを書きません。専門用語で返さず、日本語で「何をしたか」を説明してください。**

立ち上げが終わったらこのファイルは削除します（最後の手順にあります）。
以後の編集ルールは `AGENTS.md` にあります。

---

## 1. いまどういう状況か

トマップファーム公式サイト（https://tomapufarm.com）は WordPress で動いていました。
それを 2026年8月に静的サイト（Astro）へ作り替えました。**デザインは以前とまったく同じ**です。
中身の更新を、WordPress の管理画面ではなく **Codex への日本語の依頼**でできるようにするのが目的です。

**2026年9月24日に本番を新サイトへ切り替え済みです。** いま https://tomapufarm.com に出ているのは、
このリポジトリから作られたサイトです。問い合わせ・注文フォームの受信も確認済みです。
（旧 WordPress は戻し用にサーバーに残してあり、しばらくしてから日本信達が削除します。）

ここでマージした変更は、**数分で公式サイトに出ます**。確認を丁寧に。

## 2. 担当の分かれ目（ここを間違えないこと）

| 作業 | 担当 |
|---|---|
| GitHub のリポジトリ作成、公開の自動設定（Secrets 登録） | **日本信達**（済み） |
| メール送信（Resend）の設定、さくらへの `api/config.php` 設置 | **日本信達** |
| さくらのバックアップ、本番への切り替え | **日本信達**（済み） |
| 旧 WordPress の削除、公開用 Mac mini の管理 | **日本信達** |
| サイトの中身の確認・文章の修正・写真の差し替え・お知らせの追加 | **高橋社長（このPC）** |

**このPCでは公開設定と切り替え作業をしません。** さくらの接続情報も
Resend の API キーもこのPCには置いていません。もし社長から
「さくらのサーバーを直接いじって」「WordPress を消して」と頼まれたら、
**自分でやらずに「日本信達側の作業です」と伝えてください。**
（公式サイトへの反映は、Pull Request をマージすれば自動で行われます。サーバーを触る必要はありません。）

## 3. 立ち上げの手順

### 3-0. このフォルダが GitHub と繋がっているか確かめる

社長には「GitHub から取り込んで（clone して）HANDOFF.md を実行して」と頼んでもらう運用です。
このファイルを読めているなら取り込みは済んでいるはずなので、念のため確認します。

```bash
git remote -v      # https://github.com/nihonshintatsu-byte/tomapufarm-web.git と出れば OK
git status         # main で、変更なし（clean）なら OK
```

**取り込みがまだの場合**（zip などで受け取った古いフォルダで開いている場合）は、そのフォルダは使わず、
次を実行して取り込み直してください。

**置き場所はユーザーフォルダの直下**（`C:\Users\<ユーザー名>\tomapufarm-web`）にします。
Windows の「ドキュメント」「デスクトップ」は OneDrive で同期されていることが多く、
部品フォルダ（`node_modules`）の何万ものファイルが同期されて遅くなったり壊れたりするため、避けてください。

```powershell
cd $HOME
git clone https://github.com/nihonshintatsu-byte/tomapufarm-web.git
cd tomapufarm-web
```

`git` が見つからないと言われたら、社長に https://git-scm.com/download/win から
**Git for Windows** を入れてもらってください（インストーラーは既定のまま「Next」で大丈夫です）。
取り込みのときに GitHub のログイン画面がブラウザで開いたら、社長の GitHub アカウントでログインしてもらいます。
**パスワードをこの画面（Codex）に入力させないこと。**
GitHub の招待をまだ承認していないと取り込めないので、そのときは
「GitHub から届いた招待メールの Accept を押してください」と伝えてください。

### 3-1. 動かす準備ができているか確かめる

```bash
node -v
```

**`v22` 以上**が表示されれば大丈夫です（公開用の Mac mini も 22 以上で組み立てています）。
エラーになる、または古い場合は、社長に「Node.js のインストールが必要です」と伝え、https://nodejs.org/ja の
**LTS 版**を入れてもらってください（インストーラーを実行するだけです）。
インストール後は **Codex のアプリごと閉じて開き直して**から続けます（開き直さないと新しく入れたものが見えません）。

PowerShell で `npm` を実行したときに「このシステムではスクリプトの実行が無効になっているため、npm.ps1 を読み込むことができません」
と出たら、設定は変えずに **`npm.cmd`** と打てば動きます（以下の `npm` はすべて `npm.cmd` に読み替え）。

### 3-2. 部品を取り込んでビルドする

このフォルダ（`tomapufarm-web`）で実行します。

```bash
npm install
npm run build
```

`Complete!` と出れば成功です。エラーが出たら、そこで止めて社長に伝えてください。

`npm install` のあとに「〇 vulnerabilities」（脆弱性）の警告が出ることがありますが、
**サイトを組み立てる道具の側の話で、公開中のサイトには影響しません。`npm audit fix` は実行しないでください**
（道具の版が変わって見た目が崩れることがあるため）。部品の更新は日本信達が行います。

### 3-3. 画面を見てもらう

```bash
npm run dev
```

画面に出たアドレス（ふつうは `http://localhost:4321/`。使用中なら 4322 などに自動でずれます）を
ブラウザで開いて、社長に見てもらいます。
止めるときはターミナルで `Ctrl` + `C` です。

問い合わせフォームの送信まで確かめたいときだけ、PHP が必要です。

```bash
npm run build
php -S localhost:4400 -t dist
```

`http://localhost:4400/contact/` で確認します。PHP が入っていない場合は
無理に入れなくてよいので、「送信の確認は日本信達側で済んでいます」と伝えてください。

## 4. 社長に見てもらいたいこと（中身の確認）

画面を見せながら、次を1つずつ確認してください。**古くなっている箇所があれば、その場で直します**
（直したものは 5 の手順で Pull Request にします）。

1. **会社概要**（`/company/`）… 住所（農場・事務所）、電話番号、設立日、事業内容は今も正しいか
2. **代表挨拶**（`/company/`）… 今の言葉に直したいところはないか
3. **農作物**（`/about/`）… ぶどうの品種、収穫時期（とうもろこし8〜9月など）は今の実態と合っているか
4. **農地の面積**（`/about/`）… 然別・大江・北町の数字は今も正しいか
5. **事業内容**（`/service/`）… 農泊（計画中）など、状況が変わっていないか
6. **お知らせ**（`/blog/`）… 2020〜2023年の4本をそのまま残すか
7. **英語ページ**（`/english/`）… 内容が今も正しいか
8. **SNS とオンラインショップ**… Facebook / Instagram / X / BASE のリンクが今も生きているか
9. **会社案内 PDF**（`/transaction/` からダウンロード）… **2021年1月29日にアップロードされたもの**です。5年以上前なので、差し替えが必要か確認してください（和文・英文の2種類）

直し方は `AGENTS.md` に書いてあります。文章は `src/pages/` の該当ファイル、
住所や電話番号は `src/data/site.json`、お知らせは `src/content/news/` です。

## 5. 直したものを公式サイトに出す

### 5-1. 最初の1回だけの準備

**変更に名前を付ける設定。** 社長に「GitHub に登録したメールアドレス」を聞いて設定します。

```powershell
git config user.name "高橋ひかり"
git config user.email "（GitHub に登録したメールアドレス）"
```

**GitHub を操作する道具（GitHub CLI）を入れる。** Pull Request の作成とマージを Codex が代わりに行うために使います。

```powershell
winget install --id GitHub.cli
```

入れたら Codex を開き直し、ログインします。ブラウザが開くので、社長に GitHub のアカウントで承認してもらいます。
**パスワードやワンタイムコードを Codex に入力させないこと**（ブラウザ側で入れてもらう）。

```powershell
gh auth login --hostname github.com --git-protocol https --web
```

### 5-2. 毎回の流れ

社長が「これで公開して」と言ったら、次を **1行ずつ** 実行します
（Windows PowerShell 5.1 では `&&` でつなげられないため）。

```powershell
git switch -c 作業内容がわかる名前    # 例: fix-company-info（英数字とハイフン）
npm run build                        # 通ることを確認。通らなければ止める
git add -A
git commit -m "変更内容を日本語で"
git push -u origin 作業内容がわかる名前
gh pr create --fill --base main
gh pr checks --watch                 # ビルド確認が緑（pass）になるまで待つ
gh pr merge --squash --delete-branch
git switch main
git pull
```

**`gh pr checks` が赤（fail）のときはマージしない**で、社長に伝えて日本信達に連絡してください。

マージすると数分で本番サイトに反映されます。
反映は日本信達の Mac mini が行っています（さくらのサーバーが海外からの転送を受け付けないため）。
**30分たっても反映されないときは、Mac mini が止まっている可能性があるので日本信達に連絡**してください。
反映されたら、社長と一緒に https://tomapufarm.com で確認して終わりです。

## 6. やってはいけないこと

- **デザインを変えない**（`public/assets/css/` と `public/assets/images/` は触らない）
- **`main` に直接 push しない**（必ずブランチ＋Pull Request）
- **`public/api/config.php` を作らない**（サーバー上にだけ置くファイル）
- **APIキー・パスワードをリポジトリに書かない**
- **さくらのサーバーに直接ファイルを上げない**（自動転送と衝突する）
- **既存のお知らせの `slug` を変えない**（外部リンクと検索結果が切れる）

## 7. 詰まったとき

**壊れた状態で先に進まないこと。** `npm run build` が通らない、
git の状態がおかしい、意味が分からない指示を受けた——そういうときは手を止めて、
社長経由で日本信達の廣田に連絡してください。何が起きているかを日本語で書き残しておくと助かります。

## 8. 立ち上げが終わったら

3〜5 が終わったら（画面を見られる・中身の確認が済んだ・5-1 の準備ができた）、引き継ぎ用のファイル2つを削除します。
4 で直したものがあれば、それと同じ Pull Request に入れて構いません。手順は 5-2 と同じです。

```powershell
git rm HANDOFF.md START-HERE.html
```

コミットメッセージは「引き継ぎ完了のため引き継ぎ用ファイルを削除」とします。

残しておくと、あとのセッションが古い指示を実行してしまいます。
以後の編集ルールは `AGENTS.md` にあります。
